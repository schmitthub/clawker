# Substrate + File-Backed Toolchains (supersedes parts of base-image-split)

First-principles Dockerfile redesign, implemented 2026-07-06 (uncommitted, follows the base/harness split). Plan file: ~/.claude/plans/witty-churning-cocke.md (approved). Trigger: UAT `npm: not found` — old monolith rendered claude's block_1 node before root_run; split broke that implicit contract.

## Decisions (user)
- Strata: base template = clawker deps; harness template = harness deps; project config = project deps. Project config never responsible for harness; base template neither.
- BYO base image DROPPED. Substrate = `debian:bookworm-slim@sha256:60eac759...` (`bundler.SubstrateImage`, digest-pinned OCI index). `build.image`/`build.dockerfile`/`build.context` schema fields DELETED; custom-Dockerfile path deleted (post-clawkerd impossible anyway — embedded supervisor).
- Alpine support DROPPED entirely (dissolved with BYO).
- Toolchains = file-backed definitions, harness-bundle philosophy: shipped (embedded internal/bundler/assets/toolchains/<name>/, materialized copy-if-missing to <config>/toolchains/<name>, registry-seeded in settings `toolchains: {name: {path}}`), user-registered, harness-bundle-embedded (`toolchains/<name>/` in bundle). One flat namespace per build; collision = error (bundle may not shadow shipped/registry).
- Declared, never installed: project `build.toolchains: [...]` → renders in BASE before root_run (root scope) / before user_run (user scope). Harness manifest `toolchains: [...]` → harness image (root: after USER root pre-block_1; user: after USER switch + ARG ZSH_ENV hoist, pre-block_3) UNLESS project declared it (earliest stage wins, rendered once). Base never loads bundle definitions.
- Shipped v1: go(root), node(root, GPG-verified LTS float + NODE_USE_SYSTEM_CA + NODE_MIN_MAJOR=22 guard), nvm(user), rust(user, rustup), uv(user). All self-guarded (skip if present).
- Hold docs sweep + legacy deletion (master tmpl, clawker generate, BuildDefaultImage dead chain, plugin mirror) until UAT passes.

## Key code
- internal/toolchain/ (new leaf): Definition{Name,Scope,Fragment}, Load(name,fs), ValidateName; toolchain.yaml (scope: root|user) + Dockerfile.toolchain.tmpl per def.
- internal/bundler/toolchain.go: embed, ShippedToolchainNames/EnsureToolchains (mirrors EnsureHarnesses; wired in build.go beside it), resolveToolchain (bundle > registry > shipped-bootstrap; collision error), resolveProjectToolchains (bundle=nil — base harness-agnostic; dup=error), resolveHarnessToolchains (+checkBundleShadow for project-declared names), renderToolchainSteps (fragments get full *DockerfileContext).
- DockerfileContext.ToolchainRootSteps/UserSteps; static `{{range}}` anchors in both templates; nothing declared = zero bytes.
- harness.Bundle.HasToolchain/Toolchain; Manifest.Toolchains (dup/name validated at Load).
- Claude bundle: block_1 DELETED (node → shipped recipe), block_4 nvm chunk removed; harness.yaml `toolchains: [node, nvm]`. codex-harness draft: `toolchains: [node]`, block_1 dropped.
- Presets: image lines gone; python→uv, go→go, rust→rust, typescript→node declarations; java +default-jdk, ruby +ruby-full via apt; dotnet UNRESOLVED (no substrate path — flag to user).
- .clawker.yaml (dogfood): image golang gone → `toolchains: [go, node]`.
- base packages floor += ca-certificates, xz-utils (slim); isBasePackage func replaces maps.
- BaseContentHash unchanged — hashes rendered base bytes → declaration/definition edits auto-invalidate.

## Language-toolchain rework (2026-07-06, user decision)
Toolchains = LANGUAGE toolchains not tool installers: "modern node toolchain" = baked LTS + nvm; "modern python toolchain" = uv system-wide + uv-managed system python. NO dependency chains — every definition self-sufficient; cross-tool needs = declarer lists both names; `requires:` expansion deferred until real UGC need. Format change: `scope:` field DEAD; definition = toolchain.yaml (description) + optional `Dockerfile.toolchain-root.tmpl` + optional `Dockerfile.toolchain-user.tmpl` (≥1 required). Definition{Name,Description,RootFragment,UserFragment}; bundler splitFragments→namedFragment lists; placement still per-DEFINITION (project-declared node renders BOTH fragments in base). Shipped set: go(root), node(root LTS+user nvm), python(root: uv→/usr/local/bin UV_INSTALL_DIR+UV_NO_MODIFY_PATH, uv-managed CPython at UV_PYTHON_INSTALL_DIR=/usr/local/share/uv/python chmod 1777 + python3/python symlinks via `uv python find`; NO --default flag — needs preview feature per deepwiki), rust(user rustup). nvm/ + uv/ dirs deleted. Claude manifest toolchains:[node]; codex NONE (2026-07-06 correction: codex is a standalone musl rust binary via canonical chatgpt.com/codex/install.sh installer — CODEX_RELEASE pin + CODEX_NON_INTERACTIVE=1 + install-time CODEX_HOME=~/.local/share/codex-install so the ~/.codex volume can't shadow the payload/dangle the ~/.local/bin symlink; version resolution moved to new `github-release` resolver (internal/bundler/registry/github.go GitHubReleaseClient.LatestVersion + harness.ResolverNPM/GitHubRelease/None consts + VersionSpec.TagPrefix field; codex manifest: resolver github-release, package openai/codex, tag_prefix rust-v — authoritative source, npm mirror dropped); the earlier npm-install block_4 was stale-training-data hallucination — cost a day of .npmrc/nvm chasing); python preset [python]; dogfood .clawker.yaml [go,node,python]. npm/PATH model settled 2026-07-06: user-scope `npm i -g` rides the nvm default node (the installer auto-installs ARG NODE_VERSION line + default alias — now documented as deliberate in the user fragment); harness CLI binaries must land on the static image ENV PATH (~/.local/bin) because clawkerd spawns the agent CMD via direct exec.LookPath, NO login shell (spawn_unix.go) — nvm prefixes invisible to it. REJECTED: chmod 1777 /usr/local/bin (privesc — root's PATH), root npm install (codex needs no root), persisted npm prefix config (.npmrc/NPM_CONFIG_PREFIX — nvm rejects on every shell init; original codex block_4 `npm config set prefix` baked ~/.npmrc → boot-time nvm warning bug). Rationale: nvm needs baked node only because CC node resolution bugged (globs versions/node/*, ignores nvm use — anthropics/claude-code#54135); uv self-provisions python (verified live: no system python3, uv-managed cpython-3.14.6 from semgrep tool install).
UAT NOTE: host must `rm -rf <config>/toolchains` + drop settings `toolchains:` entries (old materialized single-fragment defs shadow new format → loud load error; stale uv/nvm registry names linger harmlessly but nuke for cleanliness).

## Clawker lifecycle volume (post-init marker fix, 2026-07-06)
UAT bug 2: post-init marker `~/.clawker/post-initialized` lived on container fs (overlay) while post_init effects live in config volumes → container recreation re-ran post_init → `claude mcp add` "already exists" → exit 1 forever. Fix (user decision): dedicated named volume `clawker.<proj>.<agent>-clawker` mounted at `$HOME/.clawker`. Changes: `consts.VolumePurposeClawker="clawker"` + docker re-export + `VolumePurposes`; `workspace.GetConfigVolumeMounts`/`EnsureConfigVolumes` (+`ConfigVolumeResult.ClawkerCreated`); `trackCreatedVolumes` create-abort cleanup; harness `validateVolumeSpec` reserves "clawker"; base template pre-creates `~/.clawker` chowned (volume copy-up ownership, covers no-seed harnesses; goldens re-blessed). Docker copy-up populates fresh volume with image-staged seeds/manifest; hook scripts docker-cp'd into volume at each create. Marker semantics now once-per-agent (matches config-volume lifetime); post_init re-runs only with fresh volumes.

## Status
make test 5496 green; lint 0 on touched; goldens re-blessed (alpine goldens deleted); make clawker built. UNCOMMITTED.

## UAT (host, next)
1. Delete stale materialized claude bundle: `rm -rf <config>/harnesses/claude` (copy-if-missing keeps OLD block_1/block_4 otherwise — still works but re-installs node unguarded).
2. Host `make clawker`.
3. `clawker build --no-cache`: base FROM pinned slim, go+node fragments before root_run, `npm install -g wscat` succeeds (original failure), eyeball [base] TUI.
4. `clawker build -t codex`: base skipped (hash match); codex block_4 npm works off base's node (project declared node → NOT re-rendered harness-side). Claude image renders nvm harness-side (project didn't declare nvm).
5. Host e2e: go test ./test/e2e/ -run TestPresetBuilds_E2E.

## After UAT
Legacy deletion (task list #14), docs sweep + CHANGELOG breaking notes (#15: custom-images.mdx rewrite, threat-model musl paragraph, bundler/docker/config CLAUDE.mds, release-note hazards: stale materialized bundles; build.image silently ignored). Open user items: field/file names final say, dotnet/java/ruby preset fate, plugin mirror scope, block names.
