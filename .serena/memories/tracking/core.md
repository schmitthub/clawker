# Report index

These reports preserve unresolved questions. Unchecked boxes do not establish that code is missing.
Check current source and issue status before implementation.

- Reported command failures and requested changes: `mem:tracking/bugs`.
- Runtime and firewall feature proposals: `mem:tracking/features`.
- Support-plugin behavior reports: `mem:tracking/plugin-issues`.
- Project-name and global-agent follow-up work: `mem:tracking/project-naming`.

## Known stale claims

- clawkerd now manages the user CMD as PID 1. For startup and child lifecycle rules: `mem:runtime/core`.
- BPF egress event logging now exists. This does not prove the separate traffic-metrics proposal is complete. For present ownership: `mem:controlplane/firewall/core`.
- Monitoring now uses OpenSearch and Prometheus. For current telemetry paths: `mem:monitoring/core`.
- No remaining checkbox is marked complete by this reorganization.
