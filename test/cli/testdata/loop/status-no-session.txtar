# Test loop status when no session exists
# Verifies: status command handles missing session gracefully

# Run loop status - no session should exist
exec clawker loop status --agent testdev
stderr 'No loop session found'

-- clawker.yaml --
version: "1"

build:
  image: "alpine:latest"

security:
  firewall:
    enable: false
