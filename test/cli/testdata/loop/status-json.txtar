# Test loop status --json output
# Verifies: JSON output format when no session exists

# Run loop status with --json flag
exec clawker loop status --agent testdev --json
stdout '{"exists": false}'

-- clawker.yaml --
version: "1"

build:
  image: "alpine:latest"

security:
  firewall:
    enable: false
