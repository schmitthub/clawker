version: "1"
project: "clawker-test"

build:
  image: "alpine:latest"
  packages: []

workspace:
  remote_path: "/workspace"
  default_mode: "bind"

security:
  enable_firewall: false
  docker_socket: false
