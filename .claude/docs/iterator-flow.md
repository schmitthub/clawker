❯ /pr-review-toolkit:review-pr code review the current work done and optimize based on requirements outlined in  @PRD.md tracked in recent memories about this session and testing

❯ update the current_session memory to re-open tasks related to all of these findings and report as critical task blockers that need fixing before new tasks can be started instead of doing fixes after the fact. be very descriptive. the goal is to be iterative on tasks until they meet an acceptance critieria and not move forward until everything is perfect

❯ review current_session memory and continue
