---
title: "Threat Model"
description: "Why AI agents need containment, what attack surfaces clawker mitigates, and what you're responsible for"
icon: "crosshairs"
---

AI coding agents are not traditional CLI tools. They make autonomous decisions about what files to read and write, what commands to run, what network requests to make, and what credentials to use. They do this with full conviction — even when they're wrong. They hallucinate file paths, lose context mid-task, get tunnel vision on a broken approach, and can be coerced by adversarial content embedded in the data they fetch. Handing one of these agents unrestricted code execution, full filesystem access, and open network on your bare-metal host is reckless.

Clawker exists because containers have been solving this problem for over a decade. The agent runs inside a Docker container where it can thrash, experiment, and make mistakes freely — that's the whole point of a devcontainer. What matters is that the blast radius stops at the container boundary. The host filesystem, credentials, network, and Docker daemon are protected by default, and every capability is restricted until you explicitly open it.

This page maps the attack surfaces to clawker's security controls. For configuration details and CLI commands, follow the cross-references to the dedicated guides.

## Threat Actors

Clawker's security model is designed around two threat actors:

**The agent itself.** An LLM-powered coding agent is autonomous software that can hallucinate file paths, run destructive commands, or pursue the wrong goal with absolute confidence. It doesn't need to be malicious to cause damage — context window amnesia, tunnel vision, and overconfident tool use are enough. The agent might `rm -rf` a directory it shouldn't touch, push code to the wrong branch, or install packages that compromise the environment, all while believing it's doing exactly what you asked.

**Prompt injection.** Adversarial content embedded in web pages, README files, package metadata, API responses, or files hosted on cloud storage can hijack the agent's intent. A malicious instruction hidden in a fetched document could direct the agent to exfiltrate source code, steal credentials, or modify files in ways that serve the attacker rather than the user. The agent cannot reliably distinguish legitimate instructions from injected ones.

## Data Exfiltration via Network Egress

**Threat:** A prompt-injected or hallucinating agent sends source code, secrets, environment variables, or credentials to an attacker-controlled endpoint — or leaks sensitive data to unintended services.

**Attack surface:** Any outbound network connection from the container.

**How clawker mitigates it:**

- **Deny-by-default firewall.** The Envoy+CoreDNS firewall blocks all outbound traffic unless a domain is explicitly whitelisted. DNS queries for unlisted domains return NXDOMAIN — the domain never even resolves.
- **TLS-level enforcement.** Connections to unlisted destinations are reset by Envoy. There is no fallback or soft-fail.
- **Path-level HTTP inspection.** For domains with path rules, Envoy performs MITM TLS inspection to enforce endpoint-level restrictions. The agent can reach `api.github.com/repos/` but not `api.github.com/gists` if configured that way.
- **Minimal hardcoded allowlist.** Only the Claude API and Docker registry are allowed by default. GitHub, npm, PyPI, and every other service must be explicitly added.

See the [Firewall](/firewall) guide for the full architecture, configuration, and CLI commands.

## Host Filesystem Damage

**Threat:** The agent modifies or deletes files outside the intended project scope, accesses sensitive files (`.env`, private keys, credentials) within the workspace, or operates on a directory broader than intended.

**Attack surface:** The bind-mounted workspace directory and any files accessible within it.

**How clawker mitigates it:**

- **Home directory guard.** `IsOutsideHome()` prevents mounting `$HOME` itself or directories outside `$HOME` as the workspace. The `run` and `create` commands prompt for confirmation; `loop` commands hard-error.
- **Directory-level ignore patterns.** `.clawkerignore` patterns trigger tmpfs overlays in bind mode, masking matched directories inside the container without modifying the host.
- **Snapshot mode.** Creates an ephemeral copy of the workspace in a Docker volume. Changes inside the container never reach the host. Ideal for experimental or high-risk agent tasks.
- **Unprivileged user.** Containers run as `claude:claude` (UID 1001, GID 1001), not root. The agent can only modify files the mount permissions allow.

See [Container Internals](/container-internals) for bind vs snapshot details and workspace configuration.

## Credential Theft and Misuse

**Threat:** The agent extracts SSH keys, GPG keys, API tokens, or git credentials from the container and exfiltrates them to an external endpoint. Or it uses forwarded credentials for unauthorized operations — pushing to the wrong repository, signing malicious commits, or authenticating to services it shouldn't access.

**Attack surface:** Any credential material accessible inside the container.

**How clawker mitigates it:**

- **Credentials are never stored in the container.** SSH and GPG private keys are forwarded via a socket bridge (muxrpc over `docker exec`). The key material stays on the host — only signing and authentication operations are proxied.
- **Git HTTPS credentials are transient.** The container's `git-credential-clawker` helper sends requests to the host proxy, which queries the OS keychain. Credentials pass through but are not persisted in the container.
- **Settings filtering.** Only the `enabledPlugins` key is extracted from Claude Code's `settings.json` during config volume initialization. No other settings — which might contain sensitive paths or tokens — are copied.
- **Network firewall limits exfiltration.** Even if credentials are accessed in memory, the deny-by-default firewall restricts where they can be sent.

See the [Credential Forwarding](/credentials) guide for details on each forwarding mechanism.

## Container Escape and Docker Daemon Access

**Threat:** The agent breaks out of the container to access the host filesystem, other containers, or the Docker daemon — gaining control beyond the intended sandbox.

**Attack surface:** The Docker runtime, Linux kernel namespaces, and any sockets mounted into the container.

**How clawker mitigates it:**

- **Unprivileged user.** Containers run as a non-root user. The agent cannot escalate privileges within the container.
- **Minimal capabilities.** Only `NET_ADMIN` and `NET_RAW` are added (required for firewall iptables rules). All other Linux capabilities are dropped by Docker's default seccomp profile.
- **Docker socket disabled by default.** `security.docker_socket` is `false`. Mounting the Docker socket gives the agent full control over your Docker daemon — it can create, modify, and delete any container, image, or volume on your host. This is the most direct escape path, and it's off by default.
- **Resource jailing.** `pkg/whail` decorates the Docker SDK with label-based filtering, preventing clawker from seeing or operating on non-clawker Docker resources. Even if the Docker socket were mounted, the clawker client only sees its own resources.
- **Namespace isolation.** Standard Docker PID, network, mount, and user namespace separation.

See [Security](/security#docker-socket) for Docker socket configuration and [Security](/security#linux-capabilities) for capability details.

## Prompt Injection via Fetched Content

**Threat:** An attacker places malicious instructions in content the agent fetches — a web page, a repository README, a package description, a file hosted on Google Cloud Storage or Firebase. The agent follows the injected instructions, potentially exfiltrating data, modifying code, or performing unauthorized operations.

**Attack surface:** Any domain the agent is allowed to reach. Broader allowlists mean more exposure to adversarial content.

**How clawker mitigates it:**

- **Strict domain scoping.** Only allow the specific domains your project needs. Every additional domain is additional attack surface.
- **Path-level rules.** Restrict allowed endpoints to specific API paths when possible, limiting what content the agent can fetch from allowed domains.
- **Agent awareness prompt.** Every clawker container includes `/etc/claude-code/CLAUDE.md`, which gives the agent explicit context about its sandboxed environment. This reduces the likelihood the agent follows instructions that conflict with its known constraints.
- **Network firewall limits blast radius.** Even if a prompt injection succeeds, the firewall constrains what the agent can do with it. Exfiltration attempts hit NXDOMAIN. Unauthorized API calls are blocked.

See [Firewall configuration](/firewall#configuration) for path rules and [Security](/security#agent-awareness-prompt) for the agent awareness prompt.

## Internal Network Pivoting

**Threat:** The agent uses the container as a pivot point to scan or attack internal network services — databases, APIs, admin panels, or other containers on the Docker bridge network.

**Attack surface:** The container's network connectivity to internal hosts and Docker bridge networks.

**How clawker mitigates it:**

- **Deny-default firewall.** All outbound traffic is blocked, including to internal IPs and RFC 1918 ranges.
- **DNS filtering.** Internal hostnames that are not in the allowlist do not resolve via CoreDNS.
- **Traffic redirection.** iptables DNAT rules route all HTTPS traffic through Envoy, preventing direct connections to arbitrary ports on internal hosts.
- **Dedicated Docker network.** Clawker containers run on an isolated Docker network, separated from other Docker workloads.

See [Firewall architecture](/firewall#architecture) for the network flow details.

## Certificate and TLS Interception

**Threat:** An attacker compromises the firewall's MITM CA certificate, enabling decryption of all inspected TLS traffic or impersonation of allowed domains within the firewall stack.

**Attack surface:** The self-signed CA keypair stored on the host filesystem.

**How clawker mitigates it:**

- **Per-project CA.** The CA is auto-generated per project and stored in the firewall data directory on the host — not inside agent containers.
- **CA rotation.** `clawker firewall rotate-ca` regenerates the CA and all domain certificates in one command.
- **SNI passthrough for most domains.** Domains without path rules use SNI passthrough — no MITM, no CA involvement. Only domains with path-level rules require TLS inspection.

See [Firewall certificate management](/firewall#certificate-management) for CA lifecycle and rotation.

## Summary

| Threat | Primary Control | Default | Guide |
|--------|----------------|:---:|-------|
| Data exfiltration | Network firewall (deny-all egress) | On | [Firewall](/firewall) |
| Host filesystem damage | Home guard + workspace isolation | On | [Container Internals](/container-internals) |
| Credential theft | Socket bridge + host proxy (no storage) | On | [Credentials](/credentials) |
| Container escape | Unprivileged user + no Docker socket | On | [Security](/security) |
| Prompt injection | Domain scoping + path rules | Configured per-project | [Firewall](/firewall#configuration) |
| Network pivoting | Deny-default firewall + DNS filtering | On | [Firewall](/firewall#architecture) |
| TLS interception | Per-project CA + rotation | Auto-generated | [Firewall](/firewall#certificate-management) |

## Shared Responsibility

Clawker provides the guardrails, but your configuration choices determine your actual security posture. The controls above are only as strong as how you configure them.

### Firewall Management

The firewall is your primary defense against data exfiltration. Every domain you add to the allowlist is attack surface you're accepting. Be deliberate:

- **Avoid broad IP range sources.** Adding `google` IP ranges allows traffic to all Google IPs, including Google Cloud Storage and Firebase Hosting — which serve user-generated content. An attacker can host a prompt injection payload on a public GCS bucket that your agent fetches.
- **Don't disable the firewall** unless you're in a trusted, isolated environment and accept fully unrestricted egress.
- **Use path rules** when a domain serves both needed and unnecessary endpoints.

### Credential Scoping

The credentials you inject via environment variables or env files are available to the agent for the entire session. Apply least privilege:

- Scope tokens to the minimum permissions needed. If the agent only reads repositories, use a `read`-scoped `GITHUB_TOKEN`, not `repo:write`.
- Avoid injecting long-lived secrets when short-lived or session-scoped tokens are available.
- Review what your `agent.env` and `agent.env_file` entries expose before each session.

### Docker Socket

Enabling `security.docker_socket: true` mounts the Docker socket into the container. This gives the agent full control over your Docker daemon — it can create, modify, and delete any container, image, or volume on your host. Only enable this if your workflow genuinely requires Docker-in-Docker, and understand that it effectively bypasses container isolation.

### Bypass Discipline

`clawker firewall bypass` is a temporary escape hatch, not a workflow. During a bypass, all outbound traffic goes directly to the network without filtering. Keep bypass durations short, use them only when needed, and let them auto-expire rather than running extended bypasses.

### Workspace Scope

Be intentional about what you bind-mount:

- Don't mount directories containing `.env` files, private keys, or credentials the agent doesn't need.
- Use `.clawkerignore` to mask sensitive directories.
- Use snapshot mode for experimental or high-risk agent tasks where you don't want any changes reaching the host.

### Base Image Trust

Custom build instructions (`build.instructions` in your project config) run during image build. Injected `root_run` commands execute as root. Review what you're adding — package sources, custom scripts, and copied files are part of your supply chain.

### Resource Limits

Clawker containers are throwaway devcontainers, not production workloads. Docker handles OOM kills natively, but you should configure your Docker engine's default resource limits to prevent a runaway agent from starving the host. Use `--memory` and `--cpus` flags on container creation for per-agent limits.
